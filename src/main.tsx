import React from "react";
import ReactDOM from "react-dom/client";
import ChanWeddingsStatic from "./ChanWeddingsStatic";

ReactDOM.createRoot(document.getElementById("root") as HTMLElement).render(
  <React.StrictMode>
    <ChanWeddingsStatic />
  </React.StrictMode>
);
